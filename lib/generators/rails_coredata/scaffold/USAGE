Description:
    Run rails model and controller generator for each coredata model which is from xcdatamodeld file

Example:
    rails generate rails_coredata:scaffold path/to/data.xcdatamodeld

    This will execute:
        rails generate rails_coredata:model
        rails generate rails_coredata:controller
