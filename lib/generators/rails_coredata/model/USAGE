Description:
    Run rails model generator for each coredata model which is from xcdatamodeld file

Example:
    rails generate rails_coredata:model path/to/data.xcdatamodeld

    This will execute:
        rails generate model for each coredata model
